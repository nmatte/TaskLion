var React = require('react'),
    CategoryIndex = require('./category_index');

module.exports = React.createClass({
  render: function () {
    return (
      <div>
        
        <CategoryIndex />
      </div>

    );
  }
});
